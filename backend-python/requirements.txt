# Requirements for ChessMentor with MongoDB and Authentication

# Core FastAPI
fastapi==0.104.1
uvicorn[standard]==0.24.0
websockets==12.0
python-multipart==0.0.6

# MongoDB support
motor==3.3.2
pymongo==4.6.0

# Authentication & Security
pyjwt==2.8.0
bcrypt==4.1.2

# Environment variables
python-dotenv==1.0.0

# Chess engine
python-chess==1.999

# Optional dependencies that are in your env
# stockfish (if using stockfish engine)
# openai (if using OpenAI API)